{
  "expcreate": {
    "body": "import { z } from \"zod\"\n\nexport default createRoute({\n  name: \"\",\n  params: z.object({\n    id: z.string().uuid()\n  }),\n  body: z.object(),\n  async handler({ body }) {\n    return body\n  }\n})",
    "prefix": "createRoute"
  },
  "globalThisPrisma": {
    "body": "await globalThis.prisma.$0",
    "description": "globalThis.prisma",
    "prefix": "gtp"
  }
}
